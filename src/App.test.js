import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/learn react/i); // error if no element found
  // const linkElement = screen.queryByText(/learn react/i); // null if no element found - good for testing lack of existence
  // expect(linkElement).toBeInTheDocument();
  // const element = screen.findal
});
